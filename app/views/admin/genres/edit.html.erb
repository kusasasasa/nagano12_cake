<h2>プロフィール編集</h2>
<%= form_with model: @genres , url: admin_update_genre_path(@genre.id), method: :patch do |f| %>
  <label for="inputName">Name</label>
  <%= f.text_field :name, autofocus: true, id:"inputName", placeholder: @genre.name%>
  <%= f.submit '投稿' %>
<% end %>
